# SillyTavern 1.13.5 - users.js 代码变更详解

> 专注于用户认证和存储系统的代码修改
> 
> 版本：1.13.3 → 1.13.5
> 文件：`src/users.js`

---

## 📊 变更概览

- **修改行数**：约 33 行
- **新增功能**：Authentik SSO 自动登录支持
- **优化改进**：配置路径重构、性能优化、代码重构
- **相关提交**：
  - `#4600` - Authentik automatic login support
  - `#4521` - fix(perf): disable periodic expired item check in node-persist

---

## 📋 详细代码变更

### 1. ✨ 新增 Authentik SSO 支持

#### 配置常量更新

```javascript
// 修改前（第 27 行）
const AUTHELIA_AUTH = getConfigValue('autheliaAuth', false, 'boolean');

// 修改后（第 27-28 行）
const AUTHELIA_AUTH = getConfigValue('sso.autheliaAuth', false, 'boolean');
const AUTHENTIK_AUTH = getConfigValue('sso.authentikAuth', false, 'boolean');
```

**变更说明**：
1. **Authelia 配置路径变更**：从 `autheliaAuth` 改为 `sso.autheliaAuth`
2. **新增 Authentik 配置**：`sso.authentikAuth`
3. **统一 SSO 配置命名空间**：所有 SSO 相关配置现在都在 `sso.*` 下

**配置文件对应**：
```yaml
# config.yaml
sso:
  autheliaAuth: false  # 原 autheliaAuth
  authentikAuth: false # 新增
```

---

### 2. ⚡ 存储性能优化

#### 禁用过期项周期检查

```javascript
// 文件位置：initUserStorage 函数（第 514 行）

// 修改前
await storage.init({
    dir: path.join(dataRoot, '_storage'),
    ttl: false, // Never expire
});

// 修改后
await storage.init({
    dir: path.join(dataRoot, '_storage'),
    ttl: false, // Never expire
    expiredInterval: 0,  // ✨ 新增：禁用周期性过期项检查
});
```

**变更说明**：
- **性能提升**：禁用 node-persist 的周期性过期项检查
- **原因**：`ttl: false` 表示数据永不过期，所以无需定期检查
- **效果**：减少不必要的文件系统操作，提升整体性能
- **相关提交**：`#4521`

---

### 3. 🔐 自动登录流程增强

#### tryAutoLogin 函数更新

```javascript
// 文件位置：第 720-721 行

// 修改前
export async function tryAutoLogin(request, basicAuthMode) {
    try {
        // ... 其他登录检查
        
        if (AUTHELIA_AUTH && await autheliaUserLogin(request)) {
            return true;
        }

        if (basicAuthMode && PER_USER_BASIC_AUTH && await basicUserLogin(request)) {
            return true;
        }
        
        // ...
    }
}

// 修改后
export async function tryAutoLogin(request, basicAuthMode) {
    try {
        // ... 其他登录检查
        
        if (AUTHELIA_AUTH && await autheliaUserLogin(request)) {
            return true;
        }

        // ✨ 新增：Authentik 自动登录检查
        if (AUTHENTIK_AUTH && await authentikUserLogin(request)) {
            return true;
        }

        if (basicAuthMode && PER_USER_BASIC_AUTH && await basicUserLogin(request)) {
            return true;
        }
        
        // ...
    }
}
```

**变更说明**：
- **新增 Authentik 检查**：在 Authelia 和基本认证之间插入 Authentik 检查
- **优先级**：Authelia → Authentik → 基本认证
- **条件触发**：仅当 `AUTHENTIK_AUTH` 配置为 true 时启用

---

### 4. 🔧 认证函数重构

#### 重构前的代码

```javascript
// 修改前：autheliaUserLogin 函数（约第 753 行）
/**
 * Tries auto-login with authlia trusted headers.
 * https://www.authelia.com/integration/trusted-header-sso/introduction/
 * @param {import('express').Request} request Request object
 * @returns {Promise<boolean>} Whether auto-login was performed
 */
async function autheliaUserLogin(request) {
    if (!request.session) {
        return false;
    }

    const remoteUser = request.get('Remote-User');
    if (!remoteUser) {
        return false;
    }

    const userHandles = await getAllUserHandles();
    for (const userHandle of userHandles) {
        // ... 用户查找逻辑
    }
}
```

#### 重构后的代码

```javascript
// 修改后：拆分为三个函数

/**
 * Attempts auto-login using an Authelia header.
 * https://www.authelia.com/integration/trusted-header-sso/introduction/
 * @param {import('express').Request} request Request object
 * @returns {Promise<boolean>} Whether auto-login was performed
 */
async function autheliaUserLogin(request) {
    return headerUserLogin(request, 'Remote-User');  // ✨ 委托给通用函数
}

/**
 * Attempts auto-login using an Authentik header.
 * https://docs.goauthentik.io/add-secure-apps/providers/proxy/forward_auth/
 * @param {import('express').Request} request Request object
 * @returns {Promise<boolean>} Whether auto-login was performed
 */
async function authentikUserLogin(request) {
    return headerUserLogin(request, 'X-Authentik-Username');  // ✨ 委托给通用函数
}

/**
 * Tries auto-login with a given header.
 * @param {import('express').Request} request Request object
 * @param {string} [header='Remote-User'] The header to use for the trusted user
 * @returns {Promise<boolean>} Whether auto-login was performed
 */
async function headerUserLogin(request, header = 'Remote-User') {
    if (!request.session) {
        return false;
    }

    const remoteUser = request.get(header);  // ✨ 使用参数化的 header
    if (!remoteUser) {
        return false;
    }
    console.debug(`Attempting auto-login for user from header ${header}: ${remoteUser}`);  // ✨ 新增调试日志

    const userHandles = await getAllUserHandles();
    for (const userHandle of userHandles) {
        // ... 用户查找逻辑（保持不变）
    }
}
```

**重构亮点**：

1. **DRY 原则**（Don't Repeat Yourself）
   - 提取通用逻辑到 `headerUserLogin` 函数
   - 避免代码重复

2. **可扩展性**
   - 未来添加其他基于 Header 的 SSO 只需一行代码
   - 例如：`return headerUserLogin(request, 'X-Your-SSO-Header');`

3. **新增调试日志**
   - 记录自动登录尝试
   - 包含使用的 header 名称和用户名
   - 便于问题排查

4. **更好的文档**
   - 每个函数都有清晰的 JSDoc 注释
   - 包含官方文档链接

---

## 🔑 关键变更总结

### 变更 1：配置路径重构
| 项目 | 旧路径 | 新路径 |
|------|--------|--------|
| Authelia | `autheliaAuth` | `sso.autheliaAuth` |
| Authentik | - | `sso.authentikAuth` ⭐ 新增 |

### 变更 2：新增函数
| 函数名 | 用途 | 参数 |
|--------|------|------|
| `authentikUserLogin()` | Authentik 自动登录 | `request` |
| `headerUserLogin()` | 通用 Header 登录 | `request, header='Remote-User'` |

### 变更 3：修改函数
| 函数名 | 变更类型 | 说明 |
|--------|----------|------|
| `autheliaUserLogin()` | 重构 | 委托给 `headerUserLogin()` |
| `tryAutoLogin()` | 增强 | 新增 Authentik 检查 |
| `initUserStorage()` | 优化 | 添加 `expiredInterval: 0` |

---

## 🔌 Authentik SSO 集成

### 什么是 Authentik？

Authentik 是一个开源的身份提供者（IdP），类似于 Authelia，支持：
- 单点登录（SSO）
- LDAP 提供者
- OAuth2 / OIDC
- Forward Auth（转发认证）

### 工作原理

```
┌─────────┐         ┌──────────────┐         ┌──────────────┐
│  用户   │ ──────> │   Authentik  │ ──────> │  SillyTavern │
└─────────┘         └──────────────┘         └──────────────┘
     1. 访问              2. 认证                 3. 转发 Header
                                                 X-Authentik-Username
```

1. 用户访问 SillyTavern
2. Authentik 拦截请求进行认证
3. 认证成功后，Authentik 在请求中添加 `X-Authentik-Username` Header
4. SillyTavern 读取该 Header 并自动登录对应用户

### 配置示例

#### config.yaml
```yaml
# 启用 Authentik 自动登录
sso:
  authentikAuth: true  # 启用 Authentik SSO
  autheliaAuth: false  # 如果不使用 Authelia，设为 false
```

#### Authentik 配置
在 Authentik 中设置 Forward Auth Provider，确保：
- Header 名称：`X-Authentik-Username`
- 转发到：SillyTavern 的 URL

---

## 🔄 认证流程对比

### 修改前（仅 Authelia）

```
tryAutoLogin()
  ├─ 单用户登录检查
  ├─ Authelia 登录检查 ────> autheliaUserLogin()
  └─ 基本认证登录检查            └─ 读取 Remote-User Header
```

### 修改后（Authelia + Authentik）

```
tryAutoLogin()
  ├─ 单用户登录检查
  ├─ Authelia 登录检查 ────> autheliaUserLogin()
  │                           └─ headerUserLogin(request, 'Remote-User')
  ├─ Authentik 登录检查 ───> authentikUserLogin() ⭐ 新增
  │                           └─ headerUserLogin(request, 'X-Authentik-Username')
  └─ 基本认证登录检查
                               headerUserLogin() ⭐ 通用函数
                                 ├─ 检查 session
                                 ├─ 读取指定 Header
                                 ├─ 打印调试日志
                                 └─ 查找并登录用户
```

---

## 🐛 调试和日志

### 新增调试日志

```javascript
console.debug(`Attempting auto-login for user from header ${header}: ${remoteUser}`);
```

**输出示例**：
```
Attempting auto-login for user from header X-Authentik-Username: john_doe
Attempting auto-login for user from header Remote-User: jane_smith
```

**启用调试日志**：
```bash
# 设置 NODE_ENV 或使用调试模式启动
NODE_DEBUG=* node server.js
# 或
npm run debug
```

---

## 🔒 安全注意事项

### 1. Header 信任问题

⚠️ **重要**：这些认证方法依赖于 HTTP Header，必须确保：

```
┌────────────┐
│  反向代理   │ ← 只有这里添加可信 Header
│ (Authentik) │
└─────┬──────┘
      │ X-Authentik-Username: trusted_user
      ↓
┌────────────┐
│ SillyTavern│ ← 信任来自反向代理的 Header
└────────────┘
```

**安全配置**：
- SillyTavern 必须只通过反向代理（Authentik/Authelia）访问
- 不要将 SillyTavern 直接暴露在公网
- 在反向代理中配置 Header 清理，防止用户伪造

### 2. 配置建议

```yaml
# config.yaml
listen: false          # 不要监听所有接口
whitelistMode: true    # 启用白名单模式
whitelist:
  - 127.0.0.1          # 只允许本地访问
  - ::1

sso:
  authentikAuth: true  # 通过 Authentik 访问
```

---

## 🧪 测试场景

### 测试 1：Authentik 自动登录

**步骤**：
1. 配置 Authentik Forward Auth
2. 设置 `sso.authentikAuth: true`
3. 通过 Authentik 访问 SillyTavern
4. 检查是否自动登录

**预期结果**：
- 控制台输出：`Attempting auto-login for user from header X-Authentik-Username: <username>`
- 自动登录对应用户

---

### 测试 2：配置路径兼容性

**步骤**：
1. 旧配置：`autheliaAuth: true`
2. 新配置：`sso.autheliaAuth: true`

**预期结果**：
- 需要更新配置到新路径
- 旧路径可能不再工作

---

### 测试 3：性能改进

**步骤**：
1. 启动服务器
2. 观察 `node-persist` 的文件系统操作

**预期结果**：
- 没有周期性的过期检查操作
- CPU 和 I/O 使用率降低

---

## 📝 迁移指南

### 如果您使用了 Authelia

#### 更新配置文件

```yaml
# 修改前
autheliaAuth: true

# 修改后
sso:
  autheliaAuth: true  # 移到 sso 命名空间下
```

#### 检查自定义代码

如果您的代码直接引用了这些配置：

```javascript
// 需要更新的代码
const AUTHELIA_AUTH = getConfigValue('autheliaAuth', false, 'boolean');

// 更新为
const AUTHELIA_AUTH = getConfigValue('sso.autheliaAuth', false, 'boolean');
```

---

### 如果您想使用 Authentik

#### 1. 配置 Authentik

在 Authentik 中创建 Forward Auth Provider：
- Provider 类型：Proxy
- Mode：Forward Auth
- Forward Auth Header：`X-Authentik-Username`

#### 2. 更新 SillyTavern 配置

```yaml
# config.yaml
sso:
  authentikAuth: true
```

#### 3. 配置反向代理

**Nginx 示例**：
```nginx
location / {
    # Authentik Forward Auth
    auth_request /outpost.goauthentik.io/auth/nginx;
    error_page 401 = @goauthentik_proxy_signin;
    
    # 转发认证结果
    auth_request_set $authentik_username $upstream_http_x_authentik_username;
    proxy_set_header X-Authentik-Username $authentik_username;
    
    # 代理到 SillyTavern
    proxy_pass http://localhost:8000;
}
```

#### 4. 测试自动登录

访问 SillyTavern，应该自动登录为 Authentik 中的用户。

---

## 📊 性能影响分析

### expiredInterval: 0 的影响

**修改前**：
```javascript
// node-persist 默认行为
expiredInterval: 900000  // 每 15 分钟检查一次过期项
```

**修改后**：
```javascript
expiredInterval: 0  // 完全禁用周期检查
```

**性能提升**：
- **CPU**：减少周期性的过期检查任务
- **I/O**：减少文件系统扫描操作
- **内存**：减少定时器占用
- **适用性**：由于 `ttl: false`（永不过期），检查是多余的

**适用场景**：
- ✅ 数据永不过期（如用户存储）
- ❌ 需要自动清理过期数据

---

## 🔗 相关资源

### 官方文档
- **Authentik Forward Auth**：https://docs.goauthentik.io/add-secure-apps/providers/proxy/forward_auth/
- **Authelia Trusted Headers**：https://www.authelia.com/integration/trusted-header-sso/introduction/

### 相关提交
- **#4600**：Authentik automatic login support
- **#4521**：fix(perf): disable periodic expired item check in node-persist

### 相关文件
- `src/users.js` - 用户认证和存储
- `src/config-init.js` - 配置初始化
- `config.yaml` - 配置文件

---

## ✅ 变更检查清单

### 配置更新
- [ ] 更新 `autheliaAuth` 到 `sso.autheliaAuth`
- [ ] 如需使用 Authentik，添加 `sso.authentikAuth: true`
- [ ] 验证配置文件语法正确

### 代码检查
- [ ] 检查是否有代码直接引用旧配置路径
- [ ] 更新自定义认证逻辑（如有）
- [ ] 测试自动登录功能

### 安全检查
- [ ] 确保 SillyTavern 不直接暴露
- [ ] 配置反向代理正确转发 Header
- [ ] 测试未授权访问是否被拦截

### 测试
- [ ] 测试 Authelia 自动登录（如使用）
- [ ] 测试 Authentik 自动登录（如使用）
- [ ] 检查调试日志输出
- [ ] 验证性能改进

---

## 📌 快速参考

### 配置模板

```yaml
# config.yaml - SSO 配置
sso:
  # Authelia SSO（通过 Remote-User Header）
  autheliaAuth: false
  
  # Authentik SSO（通过 X-Authentik-Username Header）
  authentikAuth: false

# 安全配置
listen: false
whitelistMode: true
whitelist:
  - 127.0.0.1
```

### 代码常量

| 常量名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `AUTHELIA_AUTH` | boolean | false | Authelia SSO 开关 |
| `AUTHENTIK_AUTH` | boolean | false | Authentik SSO 开关 |

### Header 映射

| SSO 系统 | Header 名称 | 函数 |
|----------|-------------|------|
| Authelia | `Remote-User` | `autheliaUserLogin()` |
| Authentik | `X-Authentik-Username` | `authentikUserLogin()` |

---

**文档结束**

生成时间：2025-11-01
版本：1.13.3 → 1.13.5
文件：`src/users.js`
变更行数：~33 行

