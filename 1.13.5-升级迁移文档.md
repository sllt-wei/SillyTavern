# SillyTavern 1.13.5 升级迁移文档

> 本文档用于协助二次开发代码的迁移升级工作
> 
> 版本：1.13.3 → 1.13.5
> 
> 生成时间：2025-11-01

## 📊 变更概览

- **修改文件总数**：133 个
- **新增代码**：10,237 行
- **删除代码**：2,641 行
- **净增加**：7,596 行

---

## 🎯 重点变更文件分类

### 一、前端核心文件（按修改量排序）

#### 1. 聊天和命令系统
- **`public/scripts/slash-commands.js`** (~1,024 行修改)
  - 斜杠命令翻译支持改进
  - 命令参数帮助文本本地化
  - 新增多个命令功能

- **`public/script.js`** (~655 行修改)
  - 主应用逻辑更新
  - 聊天导入批量/拖拽功能
  - 事件系统改进

- **`public/scripts/chats.js`** (~34 行修改)
  - 聊天导入增强
  - 新导入聊天高亮显示
  - swipe_info 修复

#### 2. OpenAI 和 AI 模型集成
- **`public/scripts/openai.js`** (~677 行修改，重大更新)
  - Claude Sonnet 4.5 和 Haiku 4.5 支持
  - Azure OpenAI 集成
  - Electron Hub 作为聊天补全提供者
  - 推理内容显示（NanoGPT）
  - 提示缓存改进
  - 工具调用解析修复（Gemini 多工具调用）

- **`src/endpoints/backends/chat-completions.js`** (~510 行修改，重大更新)
  - Azure OpenAI 后端支持
  - Electron Hub 集成
  - NanoGPT 推理功能
  - 深度 0 注入调整（在 continue with prefill 时）
  - 提示处理优化

#### 3. 正则表达式系统（重大功能增强）
- **`public/scripts/extensions/regex/index.js`** (~1,531 行修改)
  - **新功能：正则表达式预设系统**
  - **新功能：高级正则调试和测试面板**
  - 脚本正则表达式预设类型
  - 作用域检测重构
  - 事件系统改进
  - 引擎优化

- **`public/scripts/extensions/regex/debugger.html`** (+179 行，新文件)
  - 正则调试器用户界面

- **`public/scripts/extensions/regex/debugger.css`** (+263 行，新文件)
  - 调试器样式

- **`public/scripts/extensions/regex/engine.js`** (~254 行修改)
  - 引擎逻辑优化
  - 预设支持

#### 4. 用户界面和样式
- **`public/index.html`** (~439 行修改)
  - 布局更新
  - 新增 Persona Management id
  - 背景管理界面改进
  - 正则调试器集成

- **`public/css/backgrounds.css`** (+302 行)
  - 背景管理器重新设计
  - 现代化按钮样式
  - 文件名显示改进
  - 最大高度设置

- **`public/css/mobile-styles.css`** (~80 行修改)
  - 移动端面板固定忽略
  - 角色列表自动关闭
  - 返回按钮样式改进

- **`public/style.css`** (~204 行修改)
  - 全局样式更新
  - 无障碍改进

#### 5. 群聊和角色管理
- **`public/scripts/group-chats.js`** (~92 行修改)
  - 群聊创建时跳转修复
  - 群聊成员列表弹出窗口位置
  - 人设连接保存修复
  - 人设切换时重新打印

- **`public/scripts/personas.js`** (~61 行修改)
  - 人设描述编辑器扩展
  - freshChat 检测修复
  - 事件触发改进

#### 6. 世界书系统
- **`public/scripts/world-info.js`** (~400 行修改)
  - **新功能：Outlet 位置** - 允许将触发的世界书条目作为宏放置到任何位置
  - 允许通过 `/getpersonabook` 和 `/getcharbook` 创建新世界书
  - AN 位置修复
  - Outlet 在 AN 中的处理

#### 7. 背景和资源管理
- **`public/scripts/backgrounds.js`** (~467 行修改)
  - 背景管理器重构
  - 上传按钮添加到图库
  - 背景锁定/解锁提示移除
  - 头部布局更新
  - 键盘导航和焦点/悬停改进

#### 8. 扩展系统

##### TTS (文本转语音)
- **`public/scripts/extensions/tts/electronhub.js`** (+455 行，新文件)
  - Electron Hub TTS 提供者

- **`public/scripts/extensions/tts/index.js`** (~83 行修改)
  - 引用提取增强（支持嵌套引号）
  - Electron Hub 集成

- **`public/scripts/extensions/tts/minimax.js`** (~60 行修改)
  - MiniMax pitch 范围修复

##### 图片标注/字幕
- **`public/scripts/extensions/caption/index.js`** (~28 行修改)
  - Claude 重命名（原 Anthropic）
  - NanoGPT 作为标注源
  - Mistral 从端点加载标注模型
  - Groq 更新标注模型列表
  - Ollama 模型处理改进
  - 刷新模型按钮

##### Stable Diffusion
- **`public/scripts/extensions/stable-diffusion/index.js`** (~144 行修改)
  - 功能增强
  - 下拉菜单改进

- **`src/endpoints/stable-diffusion.js`** (+113 行)
  - 后端功能扩展

##### 其他扩展
- **`public/scripts/extensions/gallery/index.js`** (~50 行修改)
  - 上传按钮添加

- **`public/scripts/extensions/assets/index.js`** (~40 行修改)
  - 显示扩展作者

- **`public/scripts/extensions/connection-manager/index.js`** (~35 行修改)
  - 连接配置改进

#### 9. 工具函数和辅助系统
- **`public/scripts/utils.js`** (~136 行修改)
  - 工具函数增强
  - 角色更新/替换支持 URL
  - 标签重复导入过滤

- **`public/scripts/macros.js`** (~32 行修改)
  - **新增 `{{notChar}}` 宏** - 参与者列表（不包括当前角色）
  - `has()` 和 `get()` 方法添加到 MacrosParser

- **`public/scripts/util/stream-fadein.js`** (+69 行，新文件)
  - 流式输出淡入动画

- **`public/scripts/a11y.js`** (+116 行，新文件)
  - 无障碍功能支持
  - 元素角色观察器

- **`public/scripts/reasoning.js`** (~33 行修改)
  - 推理内容自动解析（模拟时）

- **`public/scripts/secrets.js`** (~40 行修改)
  - 允许保存空密钥

- **`public/scripts/tags.js`** 
  - 重复标签导入防止

- **`public/scripts/tokenizers.js`** (+55 行)
  - 分词器更新

#### 10. 其他核心改进
- **`public/scripts/popup.js`** (~22 行修改)
  - 弹窗内的 toast 位置对齐通知设置

- **`public/scripts/power-user.js`**
  - 高级用户功能更新

- **`public/scripts/preset-manager.js`** (~24 行修改)
  - 预设管理改进

- **`public/scripts/keyboard.js`**
  - Ctrl+Enter 处理（重新生成时非空输入）
  - 键盘焦点陷阱修复

- **`public/scripts/dom-handlers.js`** (+66 行)
  - DOM 处理器增强

- **`public/scripts/RossAscends-mods.js`** (~96 行修改)
  - 滚动到顶部按钮

- **`public/lib/pagination.js`** (~72 行修改)
  - 首页和末页按钮

---

### 二、后端服务器文件

#### 1. Chat Completions 后端
- **`src/endpoints/backends/chat-completions.js`** (~510 行修改，重大更新)
  - Azure OpenAI 完整集成
  - Electron Hub 支持
  - NanoGPT 集成（推理、上下文大小、视觉能力、提示缓存）
  - DeepSeek 推理器参数解锁
  - OpenRouter Google 图片输出解析
  - Gemini 2.5 Flash Image Preview
  - 深度调整优化

#### 2. OpenAI 端点
- **`src/endpoints/openai.js`** (+114 行)
  - Azure OpenAI 端点
  - 模型列表更新
  - NanoGPT 集成

#### 3. Stable Diffusion 端点
- **`src/endpoints/stable-diffusion.js`** (+113 行)
  - 新功能添加

#### 4. 安全和中间件
- **`src/middleware/hostWhitelist.js`** (+48 行，新文件)
  - **新功能：主机白名单**
  - 主机验证中间件

- **`src/users.js`** (~33 行修改)
  - Authentik 自动登录支持

#### 5. 其他后端文件
- **`src/command-line.js`** (+8 行)
  - 命令行参数扩展

- **`src/config-init.js`** (+10 行)
  - SSL 私钥密码短语支持

- **`src/constants.js`** (+41 行)
  - 新增常量定义

- **`src/endpoints/secrets.js`** (~7 行修改)
  - 空密钥保存支持

- **`src/endpoints/characters.js`**
  - 角色端点更新

- **`src/endpoints/content-manager.js`**
  - 内容管理改进

- **`src/endpoints/vectors.js`**
  - 向量端点更新

---

### 三、多语言翻译

#### 新增语言
- **`public/locales/th-th.json`** (+1,461 行)
  - 完整的泰语翻译

#### 重大更新
- **`public/locales/zh-cn.json`** (~582 行修改)
  - 简体中文翻译更新

- **`public/locales/ru-ru.json`** (~381 行修改)
  - 俄语翻译更新

#### 其他语言更新
所有其他语言文件都有小幅更新（4-6 行）

---

### 四、配置和资源文件

#### 配置
- **`default/config.yaml`** (~46 行修改)
  - 默认配置更新
  - 主机白名单配置

- **`default/content/settings.json`**
  - 默认设置更新

- **`default/content/presets/openai/Default.json`**
  - OpenAI 默认预设更新

#### 新增图片资源
- **`public/img/apple-icon-192x192.png`** (新文件)
- **`public/img/apple-icon-512x512.png`** (新文件)
- **`public/img/azure_openai.svg`** (新文件)
- **`public/img/electronhub.svg`** (新文件)

#### PWA 支持
- **`public/manifest.json`** (+10 行)
  - 添加 Firefox for Android 所需的图标尺寸

#### 错误页面
- **`default/public/error/host-not-allowed.html`** (+21 行，新文件)
  - 主机不允许错误页面

---

## 🔑 关键 API 变更

### 1. 新增 API 端点

#### Azure OpenAI
```javascript
// 新增 Azure OpenAI 聊天补全提供者
chat_completion_sources.AZURE_OPENAI = 'azure'
```

#### Electron Hub
```javascript
// 新增 Electron Hub 作为聊天补全提供者
chat_completion_sources.ELECTRONHUB = 'electronhub'
```

### 2. 新增宏

```javascript
// {{notChar}} - 返回除当前角色外的参与者列表
{{notChar}}
```

### 3. 斜杠命令更新

```javascript
// 允许创建新世界书
/getpersonabook [create]
/getcharbook [create]

// 保存空密钥
/setsecret key=""
```

### 4. 事件系统更新

```javascript
// 新增事件
event_types.SWIPE_DELETED // 滑动删除时触发，包含消息ID
event_types.MESSAGE_SWIPE_DELETED // 消息滑动删除
event_types.DRY_RUN_GENERATE // 干运行生成

// generateRaw 中触发提示事件
```

### 5. World Info 新位置

```javascript
// 新增 'outlet' 位置
world_info_position.outlet = 'outlet'
// 允许将 WI 条目作为宏放置到任何位置
```

### 6. 正则表达式预设

```javascript
// 新增预设类型
regex_placement.PRESET = 'preset'

// 正则脚本 API
getRegexScripts() // 为旧扩展重新导出
```

### 7. Custom Request 扩展

```javascript
// 在 custom-request 中新增
data.custom_prompt_post_processing = function(prompt) {
    // 自定义提示后处理
    return modifiedPrompt;
}
```

### 8. Video Avatar 扩展接口

```javascript
// 为扩展暴露
openThirdPartyExtensionMenu()
```

---

## 🔧 配置文件变更

### config.yaml 新增配置

```yaml
# 主机白名单
whitelist:
  - localhost
  - 127.0.0.1

# SSL 私钥密码短语支持
sslKey: path/to/key
sslKeyPassphrase: your_passphrase
```

---

## 📦 依赖包更新

### package.json 主要变更

```json
{
  "version": "1.13.5",
  "dependencies": {
    // 更新的包（示例）
    "@adobe/css-tools": "^4.4.4",
    "express": "^4.21.0",
    "helmet": "^8.1.0",
    "webpack": "^5.98.0"
    // ... 更多依赖更新
  }
}
```

---

## 🐛 重要 Bug 修复

### 1. 群聊相关
- 修复新建群聊时不遵守保存的人设连接
- 修复群聊切换人设时的重新打印
- 修复第一条消息缺少 swipe_info

### 2. UI/UX
- 修复 API 连接下拉菜单字体大小差异
- 修复焦点数字输入时容器滚动
- 修复键盘事件光标陷阱
- 修复流式输出时重复的 CHARACTER_MESSAGE_RENDERED 事件

### 3. AI 模型
- 修复 Gemini 多工具调用解析
- 修复 MiniMax TTS pitch 范围
- 修复创作者笔记显示字段不更新

### 4. 其他
- 防止重复标签导入
- 修复用户提示填充逻辑边界情况
- 修复聊天删除前未关闭聊天

---

## 🎨 样式和 UI 改进

### 1. 背景管理器
- 全新的现代化界面
- 改进的按钮样式
- 文件名显示优化
- 抽屉最大高度设置

### 2. 移动端
- 选择角色时自动关闭角色列表
- 隐藏面板锁定按钮
- 更大的返回按钮
- 完全忽略面板固定

### 3. 动画
- 新增流式输出淡入动画
- 改进触摸高亮效果

### 4. 弹窗
- Toast 位置对齐通知设置
- 弹窗内容改进

---

## 📚 新增功能详解

### 1. 正则表达式预设系统

这是 1.13.5 的重大功能之一。

**功能**：
- 创建可重用的正则表达式预设
- 高级调试和测试面板
- 脚本正则表达式支持
- 作用域检测

**文件**：
- `public/scripts/extensions/regex/index.js`
- `public/scripts/extensions/regex/debugger.html`
- `public/scripts/extensions/regex/debugger.css`

**使用**：
用户可以创建、保存和共享正则表达式预设，用于文本处理和转换。

### 2. 聊天批量导入和拖拽

**功能**：
- 支持批量选择多个聊天文件导入
- 支持拖拽文件到界面导入
- 导入后高亮显示新聊天

**文件**：
- `public/script.js`
- `public/scripts/chats.js`

### 3. World Info Outlet 位置

**功能**：
- 将触发的世界书条目作为宏放置到提示中的任何位置
- 使用 `{{outlet}}` 宏

**文件**：
- `public/scripts/world-info.js`

**示例**：
```
系统提示...
{{outlet}}  // 触发的 WI 条目会插入这里
角色描述...
```

### 4. Azure OpenAI 集成

**功能**：
- 完整的 Azure OpenAI 支持
- 作为聊天补全提供者

**文件**：
- `src/endpoints/backends/chat-completions.js`
- `public/scripts/openai.js`
- `public/img/azure_openai.svg`

### 5. Electron Hub 集成

**功能**：
- 作为聊天补全提供者
- TTS 支持

**文件**：
- `src/endpoints/backends/chat-completions.js`
- `public/scripts/extensions/tts/electronhub.js`
- `public/img/electronhub.svg`

### 6. 主机白名单

**功能**：
- 服务器安全增强
- 仅允许白名单中的主机访问

**文件**：
- `src/middleware/hostWhitelist.js`
- `default/public/error/host-not-allowed.html`

### 7. 无障碍功能

**功能**：
- 元素角色观察器
- 键盘导航改进

**文件**：
- `public/scripts/a11y.js`

---

## 💡 迁移建议

### 1. 如果您修改了以下文件，需要特别注意合并冲突：

- `public/script.js` - 主应用逻辑大量变更
- `public/scripts/openai.js` - OpenAI 集成重构
- `public/scripts/slash-commands.js` - 命令系统改进
- `src/endpoints/backends/chat-completions.js` - 后端重大更新
- `public/scripts/extensions/regex/index.js` - 正则系统重构

### 2. 新增依赖检查

运行以下命令更新依赖：
```bash
npm install
```

### 3. 配置文件迁移

检查您的 `config.yaml` 是否需要添加新配置项：
- 主机白名单配置
- SSL 密码短语配置

### 4. 测试重点

迁移后重点测试：
- 聊天导入功能
- 群聊功能
- 正则表达式
- AI 模型调用（特别是 Claude、Gemini）
- 世界书触发
- TTS 功能

### 5. 数据备份

升级前建议备份：
- `data/` 目录（所有用户数据）
- 自定义配置文件
- 修改过的默认文件

---

## 📄 相关文件

1. **完整代码差异文件**：`1.13.5-full-diff.patch`
   - 包含所有文件的详细 diff
   - 可使用 `git apply` 应用

2. **查看特定文件修改**：
```bash
# 查看某个文件的详细修改
git diff 4102c6af1..b2c3f749d -- path/to/file

# 查看某个目录的修改
git diff 4102c6af1..b2c3f749d -- src/endpoints/
```

---

## 🔍 按功能模块分类

### 聊天系统
- `public/scripts/chats.js`
- `public/scripts/group-chats.js`
- `public/script.js`

### AI 集成
- `public/scripts/openai.js`
- `src/endpoints/backends/chat-completions.js`
- `src/endpoints/openai.js`

### 正则表达式
- `public/scripts/extensions/regex/*`

### 背景和资源
- `public/scripts/backgrounds.js`
- `public/scripts/extensions/assets/*`
- `public/scripts/extensions/gallery/*`

### TTS
- `public/scripts/extensions/tts/*`

### 世界书
- `public/scripts/world-info.js`

### UI 和样式
- `public/index.html`
- `public/style.css`
- `public/css/*`

### 后端和安全
- `src/middleware/*`
- `src/endpoints/*`
- `src/users.js`

---

## ✅ 迁移检查清单

- [ ] 备份所有自定义代码和数据
- [ ] 审查 `1.13.5-full-diff.patch` 文件
- [ ] 识别与自定义代码的冲突点
- [ ] 更新依赖包 (`npm install`)
- [ ] 合并代码变更
- [ ] 更新配置文件
- [ ] 测试核心功能
- [ ] 测试自定义功能
- [ ] 检查控制台错误
- [ ] 验证 API 调用
- [ ] 测试移动端
- [ ] 更新文档

---

## 📞 需要帮助？

如果在迁移过程中遇到问题：

1. 查看完整 diff：`1.13.5-full-diff.patch`
2. 查看 GitHub Issues：https://github.com/SillyTavern/SillyTavern/issues
3. 查看官方文档：https://docs.sillytavern.app/

---

**文档结束**

生成时间：2025-11-01
版本：1.13.3 → 1.13.5


