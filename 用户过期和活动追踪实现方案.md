# 用户过期和活动追踪实现方案

## 📋 需求说明

在 `setUserDataMiddleware` 中添加：
1. 用户过期时间检查
2. 过期后自动登出并重定向
3. 更新用户最后活动时间
4. 在线用户管理

---

## 🔧 实现方案

### 步骤 1：更新 User 类型定义

在 `src/users.js` 文件开头的 User typedef 中添加新字段（约第 48-57 行）：

```javascript
/**
 * @typedef {Object} User
 * @property {string} handle - The user's short handle. Used for directories and other references
 * @property {string} name - The user's name. Displayed in the UI
 * @property {number} created - The timestamp when the user was created
 * @property {string} password - Scrypt hash of the user's password
 * @property {string} salt - Salt used for hashing the password
 * @property {boolean} enabled - Whether the user is enabled
 * @property {boolean} admin - Whether the user is an admin (can manage other users)
 * @property {number} [expiry] - Optional expiry timestamp (milliseconds). If set, user will be logged out after this time
 * @property {number} [lastActivity] - Last activity timestamp (milliseconds)
 */
```

---

### 步骤 2：添加在线用户跟踪

在常量定义区域（约第 36 行之后）添加：

```javascript
/**
 * Cache for user directories.
 * @type {Map<string, UserDirectoryList>}
 */
const DIRECTORIES_CACHE = new Map();
const PUBLIC_USER_AVATAR = '/img/default-user.png';
const COOKIE_SECRET_PATH = 'cookie-secret.txt';

// ✨ 新增：在线用户跟踪
/**
 * Set of currently online users (by handle).
 * @type {Set<string>}
 */
const ONLINE_USERS = new Set();
```

---

### 步骤 3：添加辅助函数

在 `setUserDataMiddleware` 函数之前添加这些辅助函数：

```javascript
/**
 * Updates the last activity timestamp for a user.
 * @param {string} handle - User handle
 * @returns {Promise<void>}
 */
async function updateUserActivity(handle) {
    if (!handle) return;
    
    try {
        const user = await storage.getItem(toKey(handle));
        if (user) {
            user.lastActivity = Date.now();
            await storage.setItem(toKey(handle), user);
            
            // 同时添加到在线用户集合
            ONLINE_USERS.add(handle);
        }
    } catch (error) {
        console.error('Failed to update user activity:', handle, error);
    }
}

/**
 * Removes a user from the online users set.
 * @param {string} handle - User handle
 */
function removeOnlineUser(handle) {
    if (!handle) return;
    ONLINE_USERS.delete(handle);
}

/**
 * Gets the list of currently online users.
 * @returns {string[]} Array of user handles
 */
export function getOnlineUsers() {
    return Array.from(ONLINE_USERS);
}

/**
 * Checks if a user is online.
 * @param {string} handle - User handle
 * @returns {boolean}
 */
export function isUserOnline(handle) {
    return ONLINE_USERS.has(handle);
}

/**
 * Clears the user session and removes from online users.
 * @param {import('express').Request} request - Request object
 * @param {string} handle - User handle
 */
function clearUserSession(request, handle) {
    if (handle) {
        removeOnlineUser(handle);
    }
    
    if (request.session) {
        request.session.handle = null;
        request.session.csrfToken = null;
        request.session.destroy((err) => {
            if (err) {
                console.error('Failed to destroy session:', err);
            }
        });
    }
}
```

---

### 步骤 4：修改 setUserDataMiddleware 函数

将原来的 `setUserDataMiddleware` 函数（第 850-900 行）替换为：

```javascript
/**
 * Middleware to add user data to the request object.
 * Also checks user expiry and updates activity.
 * @param {import('express').Request} request Request object
 * @param {import('express').Response} response Response object
 * @param {import('express').NextFunction} next Next function
 */
export async function setUserDataMiddleware(request, response, next) {
    // If user accounts are disabled, use the default user
    if (!ENABLE_ACCOUNTS) {
        const handle = DEFAULT_USER.handle;
        const directories = getUserDirectories(handle);
        request.user = {
            profile: DEFAULT_USER,
            directories: directories,
        };
        return next();
    }

    if (!request.session) {
        console.error('Session not available');
        return response.sendStatus(500);
    }

    // If user accounts are enabled, get the user from the session
    let handle = request.session?.handle;

    // If we have the only user and it's not password protected, use it
    if (!handle) {
        return next();
    }

    /** @type {User} */
    const user = await storage.getItem(toKey(handle));

    if (!user) {
        console.error('User not found:', handle);
        clearUserSession(request, handle);
        return next();
    }

    if (!user.enabled) {
        console.error('User is disabled:', handle);
        clearUserSession(request, handle);
        return next();
    }

    // ✨ 新增：检查用户是否过期
    if (user.expiry && user.expiry < Date.now()) {
        console.warn(`User ${user.handle} has expired. Logging out.`);
        
        // 清除会话和在线状态
        clearUserSession(request, handle);
        
        // 重定向到登录页并显示过期消息
        return response.redirect('/login?error=expired');
    }

    // ✨ 新增：更新用户最后活动时间（异步执行，不阻塞请求）
    // 只在 GET 请求时更新，避免频繁写入
    if (request.method === 'GET') {
        updateUserActivity(handle).catch(err => {
            console.error('Failed to update user activity:', err);
        });
    }

    const directories = getUserDirectories(handle);
    request.user = {
        profile: user,
        directories: directories,
    };

    // Touch the session if loading the home page
    if (request.method === 'GET' && request.path === '/') {
        request.session.touch = Date.now();
    }

    return next();
}
```

---

### 步骤 5：在登出时清理在线用户

找到登出相关的函数并添加清理逻辑。在 `src/users.js` 中查找登出端点：

```javascript
// 在登出路由处理器中添加（可能在 initUserStorage 函数后面）
router.post('/logout', csrfProtection, async (request, response) => {
    const handle = request.session?.handle;
    
    // ✨ 添加：清理在线用户状态
    if (handle) {
        removeOnlineUser(handle);
    }
    
    if (request.session) {
        request.session.destroy((err) => {
            if (err) {
                console.error('Failed to destroy session:', err);
                return response.sendStatus(500);
            }
            response.redirect('/login');
        });
    } else {
        response.redirect('/login');
    }
});
```

---

### 步骤 6：添加管理端点（可选）

如果需要查看在线用户和用户活动状态，可以添加管理端点：

```javascript
/**
 * Route handler to get online users (admin only).
 */
router.get('/api/users/online', requireLoginMiddleware, async (request, response) => {
    try {
        // 检查是否是管理员
        if (!request.user?.profile?.admin) {
            return response.sendStatus(403);
        }
        
        const onlineHandles = getOnlineUsers();
        const onlineUsers = [];
        
        for (const handle of onlineHandles) {
            const user = await storage.getItem(toKey(handle));
            if (user) {
                onlineUsers.push({
                    handle: user.handle,
                    name: user.name,
                    lastActivity: user.lastActivity,
                });
            }
        }
        
        response.json({
            count: onlineUsers.length,
            users: onlineUsers,
        });
    } catch (error) {
        console.error('Failed to get online users:', error);
        response.sendStatus(500);
    }
});

/**
 * Route handler to set user expiry (admin only).
 */
router.post('/api/users/:handle/expiry', requireLoginMiddleware, async (request, response) => {
    try {
        // 检查是否是管理员
        if (!request.user?.profile?.admin) {
            return response.sendStatus(403);
        }
        
        const { handle } = request.params;
        const { expiry } = request.body; // timestamp in milliseconds or null to clear
        
        const user = await storage.getItem(toKey(handle));
        if (!user) {
            return response.status(404).json({ error: 'User not found' });
        }
        
        // 设置或清除过期时间
        if (expiry === null || expiry === undefined) {
            delete user.expiry;
        } else {
            user.expiry = Number(expiry);
        }
        
        await storage.setItem(toKey(handle), user);
        
        response.json({
            success: true,
            handle: user.handle,
            expiry: user.expiry,
        });
    } catch (error) {
        console.error('Failed to set user expiry:', error);
        response.sendStatus(500);
    }
});
```

---

## 🎨 前端登录页面错误提示

在 `default/public/error/` 或登录页面中添加过期错误提示：

```javascript
// 在登录页面的 JavaScript 中
const urlParams = new URLSearchParams(window.location.search);
const error = urlParams.get('error');

if (error === 'expired') {
    // 显示用户过期提示
    toastr.warning('您的账户已过期，请重新登录或联系管理员。', '会话已过期');
}
```

---

## 📝 使用示例

### 设置用户过期时间

```javascript
// 设置用户 24 小时后过期
const user = await storage.getItem(toKey('username'));
user.expiry = Date.now() + (24 * 60 * 60 * 1000); // 24 hours
await storage.setItem(toKey('username'), user);
```

### 清除用户过期时间

```javascript
const user = await storage.getItem(toKey('username'));
delete user.expiry;
await storage.setItem(toKey('username'), user);
```

### 检查在线用户

```javascript
import { getOnlineUsers, isUserOnline } from './users.js';

// 获取所有在线用户
const onlineUsers = getOnlineUsers();
console.log('Online users:', onlineUsers);

// 检查特定用户是否在线
if (isUserOnline('username')) {
    console.log('User is online');
}
```

---

## ⚙️ 配置选项（可选）

可以在 `config.yaml` 中添加相关配置：

```yaml
# 用户会话配置
userSessions:
  # 用户活动更新间隔（秒）
  # 避免频繁写入，只在距离上次更新超过此间隔时才更新
  activityUpdateInterval: 300  # 5 分钟
  
  # 默认用户过期时间（小时），0 表示永不过期
  defaultExpiryHours: 0
  
  # 是否在用户过期后自动禁用账户
  disableOnExpiry: false
```

然后在代码中读取配置：

```javascript
const ACTIVITY_UPDATE_INTERVAL = getConfigValue('userSessions.activityUpdateInterval', 300, 'number') * 1000;

// 在 updateUserActivity 函数中
async function updateUserActivity(handle) {
    if (!handle) return;
    
    try {
        const user = await storage.getItem(toKey(handle));
        if (user) {
            const now = Date.now();
            // 只在距离上次更新超过指定间隔时才更新
            if (!user.lastActivity || now - user.lastActivity > ACTIVITY_UPDATE_INTERVAL) {
                user.lastActivity = now;
                await storage.setItem(toKey(handle), user);
            }
            
            ONLINE_USERS.add(handle);
        }
    } catch (error) {
        console.error('Failed to update user activity:', handle, error);
    }
}
```

---

## 🧪 测试步骤

### 1. 测试用户过期

```javascript
// 设置用户 1 分钟后过期（用于测试）
const user = await storage.getItem(toKey('testuser'));
user.expiry = Date.now() + 60000; // 1 minute
await storage.setItem(toKey('testuser'), user);

// 1 分钟后访问任何页面，应该被重定向到 /login?error=expired
```

### 2. 测试活动时间更新

```javascript
// 登录后
const user = await storage.getItem(toKey('testuser'));
console.log('Last activity:', new Date(user.lastActivity));

// 访问几个页面后
const updatedUser = await storage.getItem(toKey('testuser'));
console.log('Updated activity:', new Date(updatedUser.lastActivity));
```

### 3. 测试在线用户

```javascript
import { getOnlineUsers, isUserOnline } from './src/users.js';

// 用户登录后
console.log('Is online:', isUserOnline('testuser')); // should be true
console.log('Online users:', getOnlineUsers());

// 用户登出后
console.log('Is online:', isUserOnline('testuser')); // should be false
```

---

## ⚠️ 注意事项

### 1. 性能考虑

- **活动时间更新频率**：建议设置更新间隔，避免每个请求都写入存储
- **只在 GET 请求时更新**：POST/PUT/DELETE 请求通常不需要更新活动时间
- **异步更新**：使用 `.catch()` 而不是 `await`，避免阻塞请求

### 2. 时区问题

- 所有时间戳使用 `Date.now()`（UTC 毫秒）
- 显示时注意转换为用户本地时区

### 3. 在线用户状态

- `ONLINE_USERS` 是内存中的 Set，服务器重启后会清空
- 如果需要持久化，考虑使用数据库或定期清理不活跃用户

### 4. 会话清理

- 使用 `request.session.destroy()` 而不是简单的 `null`
- 确保清理所有相关状态（CSRF token、在线状态等）

---

## 🔄 完整代码位置

| 修改位置 | 行号（大约） | 说明 |
|---------|------------|------|
| User typedef | 48-57 | 添加 expiry 和 lastActivity 字段 |
| 常量定义 | 36+ | 添加 ONLINE_USERS Set |
| 辅助函数 | 820-850 | 添加活动和在线用户管理函数 |
| setUserDataMiddleware | 850-900 | 添加过期检查和活动更新 |
| 登出处理 | 搜索 logout | 添加在线用户清理 |
| 管理端点（可选） | 文件末尾 | 添加在线用户查询端点 |

---

## 📦 完整修改后的代码片段

查看下一条消息获取完整的代码实现...

