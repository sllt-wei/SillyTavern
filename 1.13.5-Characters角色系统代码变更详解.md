# SillyTavern 1.13.5 - Characters 角色系统代码变更详解

> 专注于角色/人设/群聊相关的代码修改
> 
> 版本：1.13.3 → 1.13.5

---

## 📋 目录

1. [后端 - 角色端点变更](#后端---角色端点变更)
2. [人设系统 (Personas) 重大更新](#人设系统-personas-重大更新)
3. [群聊系统变更](#群聊系统变更)
4. [角色卡相关工具函数](#角色卡相关工具函数)
5. [世界书与角色集成](#世界书与角色集成)
6. [相关提交记录](#相关提交记录)

---

## 后端 - 角色端点变更

### 文件：`src/endpoints/characters.js`

#### 1. 缓存系统优化

```javascript
// 修改前
dir: this.cachePath,
ttl: false,
forgiveParseErrors: true,
// @ts-ignore
maxFileDescriptors: 100,

// 修改后
dir: this.cachePath,
ttl: false,
forgiveParseErrors: true,
expiredInterval: 0,  // ✨ 新增：禁用周期性过期项检查，提升性能
// @ts-ignore
maxFileDescriptors: 100,
```

**说明**：
- 禁用 node-persist 的周期性过期项检查
- 提升性能，减少不必要的文件系统操作
- 相关提交：`#4521`

---

#### 2. 世界书导出支持 Outlet

```javascript
// convertWorldInfoToCharacterBook 函数更新
function convertWorldInfoToCharacterBook(name, entries) {
    // ... 省略部分代码
    
    entries.map((entry) => {
        return {
            // ... 其他字段
            useProbability: entry.useProbability ?? false,
            depth: entry.depth ?? 4,
            selectiveLogic: entry.selectiveLogic ?? 0,
            outlet_name: entry.outletName ?? '',  // ✨ 新增：支持 outlet 位置
            group: entry.group ?? '',
            group_override: entry.groupOverride ?? false,
            group_weight: entry.groupWeight ?? null,
            // ... 其他字段
        };
    });
}
```

**说明**：
- 世界书条目转换为角色书时，支持导出 `outlet_name` 字段
- 这与新的 World Info Outlet 位置功能配套
- 相关提交：`#4523`

---

## 人设系统 (Personas) 重大更新

### 文件：`public/scripts/personas.js`

这个文件有约 **61 行修改**，涉及多个重要改进。

---

### 1. 防止重复加载人设

#### 新增变量和逻辑

```javascript
/** @type {string} The last loaded chat id to remember for persona loading */
let personaLastLoadedChatId = null;

async function loadPersonaForCurrentChat({ doRender = false } = {}) {
    // ✨ 新增：检查是否是同一个聊天，避免重复加载
    const currentChatId = getCurrentChatId();
    if (currentChatId === personaLastLoadedChatId) return;
    personaLastLoadedChatId = currentChatId;
    
    // ... 其余代码
}
```

**说明**：
- 防止在同一个聊天中重复触发人设加载
- 提升性能，避免不必要的状态更新
- 修复了群聊切换时的重复加载问题

---

### 2. 异步化人设切换

#### `setUserAvatar` 函数更新

```javascript
// 修改前
export function setUserAvatar(imgfile, { toastPersonaNameChange = true, navigateToCurrent = false } = {}) {
    user_avatar = imgfile && typeof imgfile === 'string' ? imgfile : $(this).attr('data-avatar-id');
    reloadUserAvatar();
    updatePersonaUIStates({ navigateToCurrent: navigateToCurrent });
    selectCurrentPersona({ toastPersonaNameChange: toastPersonaNameChange });
    retriggerFirstMessageOnEmptyChat();
    saveSettingsDebounced();
    $('.zoomed_avatar[forchar]').remove();
}

// 修改后
export async function setUserAvatar(imgfile, { toastPersonaNameChange = true, navigateToCurrent = false } = {}) {
    // ✨ 新增：记录当前人设，避免重复设置
    const currentUserAvatar = user_avatar;
    user_avatar = imgfile && typeof imgfile === 'string' ? imgfile : $(this).attr('data-avatar-id');
    if (currentUserAvatar === user_avatar) {
        return;  // 如果人设没变，直接返回
    }
    reloadUserAvatar();
    updatePersonaUIStates({ navigateToCurrent: navigateToCurrent });
    selectCurrentPersona({ toastPersonaNameChange: toastPersonaNameChange });
    await retriggerFirstMessageOnEmptyChat();  // ✨ 改为 await
    saveSettingsDebounced();
    $('.zoomed_avatar[forchar]').remove();
}
```

**关键变更**：
1. **函数改为 async**：支持异步操作
2. **重复检测**：如果人设没变，直接返回，避免不必要的操作
3. **await 调用**：等待 `retriggerFirstMessageOnEmptyChat` 完成

---

### 3. 改进的首消息重新触发逻辑

#### `retriggerFirstMessageOnEmptyChat` 函数重构

```javascript
// 修改前
export function retriggerFirstMessageOnEmptyChat() {
    if (Number(this_chid) >= 0 && !selected_group && chat.length === 1) {
        $('#firstmessage_textarea').trigger('input');
    }
}

// 修改后
export async function retriggerFirstMessageOnEmptyChat() {
    // ✨ 新增：检查聊天是否被污染（tainted）
    if (chat_metadata.tainted) {
        return;
    }
    // ✨ 新增：群聊时重新加载整个聊天
    if (selected_group) {
        await reloadCurrentChat();
    }
    // ✨ 修改：单聊时重新创建/编辑角色
    if (!selected_group && Number(this_chid) >= 0 && chat.length === 1) {
        await createOrEditCharacter();
    }
}
```

**重大变更**：
1. **tainted 检查**：如果聊天已被"污染"（用户已编辑过），不重新触发
2. **群聊支持**：群聊时重新加载整个聊天，而不是只触发首消息
3. **更可靠的更新**：使用 `createOrEditCharacter()` 而不是触发输入事件
4. **相关提交**：`#4603` - Reprint non-tainted group chat on switching personas

---

### 4. 自动选择人设优化

#### `autoSelectPersona` 函数异步化

```javascript
// 修改前
export function autoSelectPersona(name) {
    for (const [key, value] of Object.entries(power_user.personas)) {
        if (value === name) {
            console.log(`Auto-selecting persona ${key} for name ${name}`);
            setUserAvatar(key);
            return true;
        }
    }
    return false;
}

// 修改后
export async function autoSelectPersona(name) {
    for (const [key, value] of Object.entries(power_user.personas)) {
        if (value === name) {
            console.log(`Auto-selecting persona ${key} for name ${name}`);
            await setUserAvatar(key);  // ✨ 改为 await
            return true;
        }
    }
    return false;
}
```

**说明**：
- 配合 `setUserAvatar` 的异步化
- 确保人设切换完全完成后再返回

---

### 5. 人设描述编辑器修复

```javascript
// 修改前
function onPersonaDescriptionInput() {
    // ... 保存逻辑
    
    $(`.avatar-container[imgfile="${user_avatar}"] .ch_description`)
        .text(power_user.persona_description || $('#user_avatar_block').attr('no_desc_text'))
        .toggleClass('text_muted', !power_user.persona_description);
    saveSettingsDebounced();
}

// 修改后
function onPersonaDescriptionInput() {
    // ... 保存逻辑
    
    // ✨ 修复：使用 data-avatar-id 而不是 imgfile
    $(`.avatar-container[data-avatar-id="${user_avatar}"] .ch_description`)
        .text(power_user.persona_description || $('#user_avatar_block').attr('no_desc_text'))
        .toggleClass('text_muted', !power_user.persona_description);
    saveSettingsDebounced();
}
```

**说明**：
- 修复选择器，使用正确的属性 `data-avatar-id`
- 相关提交：`#4591` - Persona description editor expand

---

### 6. 人设连接检测优化

```javascript
// 修改前
export function getConnectedPersonas(characterKey = undefined) {
    characterKey ??= selected_group || characters[Number(this_chid)]?.avatar;
    const connectedPersonas = Object.entries(power_user.persona_descriptions)
        .filter(([_, desc]) => desc.connections?.some(conn => conn.type === 'character' && conn.id === characterKey))
        .map(([key, _]) => key);
    return connectedPersonas;
}

// 修改后
export function getConnectedPersonas(characterKey = undefined) {
    characterKey ??= selected_group || characters[Number(this_chid)]?.avatar;
    const connectedPersonas = Object.entries(power_user.persona_descriptions)
        // ✨ 简化：移除 type === 'character' 检查，直接匹配 id
        .filter(([_, { connections }]) => connections?.some(conn => conn.id === characterKey))
        .map(([key, _]) => key);
    return connectedPersonas;
}
```

**说明**：
- 简化连接检测逻辑
- 使用解构语法更清晰
- 相关提交：`#4594` - Fix group chat on new chat not respecting saved persona connections

---

### 7. 人设锁定异步化

```javascript
// 修改后：多处锁定调用改为 await
async function loadPersonaForCurrentChat({ doRender = false } = {}) {
    // ... 省略代码
    
    // ✨ 异步锁定
    if (chatPersona && power_user.persona_auto_lock && !chat_metadata['persona']) {
        await lockPersona('chat');
    }
    
    // ... 省略代码
}
```

---

### 8. 斜杠命令更新

```javascript
// 修改前
function setNameCallback({ mode = 'all' }, name) {
    // ... 逻辑
    if (persona) {
        autoSelectPersona(persona);
        return '';
    }
}

// 修改后
async function setNameCallback({ mode = 'all' }, name) {
    // ... 逻辑
    if (persona) {
        await autoSelectPersona(persona);  // ✨ 改为 await
        return '';
    }
}
```

**说明**：
- `/name` 命令支持异步操作
- 确保人设切换完成

---

### 9. 事件监听器更新

```javascript
// 修改前
$(document).on('click', '#user_avatar_block .avatar-container', function () {
    const imgfile = $(this).attr('data-avatar-id');
    setUserAvatar(imgfile);
});

// 修改后
$(document).on('click', '#user_avatar_block .avatar-container', async function () {
    const imgfile = $(this).attr('data-avatar-id');
    await setUserAvatar(imgfile);  // ✨ 改为 await
});
```

---

### 10. 导出新函数

```javascript
// 修改前
function getOrCreatePersonaDescriptor() {
    // ...
}

// 修改后
export function getOrCreatePersonaDescriptor() {  // ✨ 导出供外部使用
    // ...
}
```

**说明**：
- 允许其他模块获取或创建人设描述符
- 增强模块间的互操作性

---

### 11. 新增导入

```javascript
import {
    // ... 原有导入
    createOrEditCharacter,    // ✨ 新增
    getCurrentChatId,         // ✨ 新增
    // ... 其他导入
}
```

---

## 群聊系统变更

### 文件：`public/scripts/group-chats.js`

约 **92 行修改**

### 主要修改点

#### 1. 群聊人设连接保存修复

**相关提交**：`#4594` - Fix group chat on new chat not respecting saved persona connections

**问题**：新建群聊时不遵守保存的人设连接

**修复**：确保在创建新群聊时正确加载和应用人设连接

---

#### 2. 群聊成员列表弹出窗口位置

**相关提交**：`#4649` - groupMemberListPopout placement

**修改**：调整群聊成员列表弹出窗口的显示位置，改善用户体验

---

#### 3. 新建群聊跳转修复

**相关提交**：`#4602` - Fix not jumping twice to group chat when creating a new group chat with deleting the old one

**问题**：删除旧群聊并创建新群聊时，不会跳转两次

**修复**：确保创建新群聊时正确跳转

---

#### 4. 人设切换时重新打印

**相关提交**：`#4603` - Reprint non-tainted group chat on switching personas

```javascript
// 在 retriggerFirstMessageOnEmptyChat 中
if (selected_group) {
    await reloadCurrentChat();  // 群聊时重新加载聊天
}
```

**说明**：
- 切换人设时，如果在群聊中且聊天未被污染，重新打印整个聊天
- 确保群聊消息正确反映新人设

---

## 角色卡相关工具函数

### 文件：`public/scripts/utils.js`

### 1. 角色导入/导出增强

#### 拖拽文件处理

```javascript
// 新增导入
import { characters, getRequestHeaders, processDroppedFiles, this_chid, user_avatar } from '../script.js';

// 在文件上传函数中
switch (customContentType) {
    case 'character':  // ✨ 处理角色卡拖拽
        await processDroppedFiles([file], extraData);
        break;
    case 'lorebook':   // 处理世界书拖拽
        await importWorldInfo(file);
        break;
}
```

**说明**：
- 改进拖拽导入角色卡的逻辑
- 支持直接拖拽角色卡文件到界面

---

### 2. 角色更新/替换功能扩展

**相关提交**：`#4581` - Extend Update/Replace character function to replace directly from URL + Fixes on cached thumbnail

**新功能**：
- 支持直接从 URL 替换角色
- 修复缓存缩略图问题

---

### 3. 角色源链接显示优化

**相关提交**：
- `#4588` - Small change, (visually) disable "Link to Source" on characters that weren't downloaded from a source
- `#4592` - fix: enable character source (open link) when group is selected

**修改**：
- 如果角色不是从源下载的，视觉上禁用"链接到源"按钮
- 选中群组时也能使用角色源链接功能

---

## 世界书与角色集成

### 文件：`public/scripts/world-info.js`

约 **400 行修改**

### 1. 通过斜杠命令创建世界书

**相关提交**：`#4597` - Allow create new world books via `/getpersonabook` and `/getcharbook`

```javascript
// 新增功能
/getpersonabook [create]  // 为当前人设创建新世界书
/getcharbook [create]     // 为当前角色创建新世界书
```

**说明**：
- 之前这些命令只能获取现有世界书
- 现在可以使用 `create` 参数创建新世界书

---

### 2. Outlet 位置支持

**相关提交**：`#4523` - Add new WI position 'outlet' to expose WI entries triggered to be placed as macros literally anywhere

**新增世界书位置类型**：

```javascript
// 新增 outlet 位置
world_info_position.outlet = 'outlet'
```

**用法**：
```
系统提示...
{{outlet:outlet_name}}  // 触发的特定 outlet WI 条目插入这里
角色描述...
```

**说明**：
- 允许将触发的世界书条目作为宏放置到提示中的任何位置
- 每个条目可以指定自己的 outlet_name
- 在提示中使用 `{{outlet:name}}` 宏来引用

---

## 其他角色相关修改

### 1. 创作者笔记显示修复

**相关提交**：`#4552` - Fix creator notes display field (in char panel) not updating when changed in Advanced Definitions

**问题**：在高级定义中修改创作者笔记后，角色面板中的显示字段不更新

**修复**：确保创作者笔记字段实时同步更新

---

### 2. 角色消息渲染事件

**相关提交**：`#4584` - Fix duplicate CHARACTER_MESSAGE_RENDERED events during streaming

**问题**：流式输出时触发重复的 `CHARACTER_MESSAGE_RENDERED` 事件

**修复**：确保每条消息只触发一次渲染完成事件

---

### 3. first_mes 安全访问

**相关提交**：`#4601` - fix: add optional chaining to prevent undefined error when accessing first_mes

**修复**：
```javascript
// 修改前
const firstMessage = character.first_mes;

// 修改后
const firstMessage = character?.first_mes;  // ✨ 使用可选链
```

**说明**：防止访问不存在角色的 first_mes 时出错

---

## 完整相关提交列表

按时间顺序（最新在前）：

1. **#4603** - `9463d1a15` - Reprint non-tainted group chat on switching personas
2. **#4597** - `a65f708fd` - Allow create new world books via `/getpersonabook` and `/getcharbook`
3. **#4594** - `06f5cea40` - Fix group chat on new chat not respecting saved persona connections
4. **#4592** - `08f48a986` - fix: enable character source (open link) when group is selected
5. **#4591** - `334a5409a` - Persona description editor expand
6. **#4588** - `3e3dfbe05` - Small change, (visually) disable "Link to Source" on characters that weren't downloaded from a source
7. **#4584** - `58a294639` - Fix duplicate CHARACTER_MESSAGE_RENDERED events during streaming
8. **#4581** - `7bedaed92` - Extend Update/Replace character function to replace directly from URL + Fixes on cached thumbnail
9. **#4552** - `c9419a383` - Fix creator notes display field (in char panel) not updating when changed in Advanced Definitions
10. **#4523** - `7cba4e88a` - Add new WI position 'outlet' to expose WI entries triggered to be placed as macros literally anywhere
11. **#4521** - `e0edd5765` - fix(perf): disable periodic expired item check in node-persist

---

## 迁移指南

### 如果您自定义了人设系统

1. **检查异步调用**：所有调用 `setUserAvatar` 的地方现在需要 `await`
   ```javascript
   // 修改前
   setUserAvatar(avatarId);
   
   // 修改后
   await setUserAvatar(avatarId);
   ```

2. **检查事件监听器**：确保异步操作完成
   ```javascript
   // 如果您有自定义的人设切换逻辑
   $(document).on('click', '.my-persona-button', async function () {
       await setUserAvatar(avatarId);  // 使用 await
   });
   ```

3. **tainted 状态**：如果您的代码依赖 `retriggerFirstMessageOnEmptyChat`，注意它现在会检查 `chat_metadata.tainted`

---

### 如果您自定义了群聊功能

1. **人设连接**：检查群聊创建时的人设连接逻辑是否与您的自定义兼容

2. **重新打印逻辑**：人设切换时会触发 `reloadCurrentChat()`，确保您的自定义不会冲突

---

### 如果您使用了世界书

1. **Outlet 位置**：
   - 检查您的世界书条目是否需要使用新的 outlet 功能
   - 更新您的提示模板以使用 `{{outlet:name}}` 宏

2. **导出角色**：
   - 导出的角色卡现在会包含 `outlet_name` 字段
   - 如果您有自定义导入/导出逻辑，需要处理这个字段

---

### 测试建议

测试以下场景：

1. ✅ 人设切换（单聊）
2. ✅ 人设切换（群聊）
3. ✅ 新建聊天时自动选择人设
4. ✅ 人设连接保存和加载
5. ✅ 群聊创建
6. ✅ 角色导入/导出
7. ✅ 世界书创建（通过斜杠命令）
8. ✅ 创作者笔记编辑和显示
9. ✅ 拖拽导入角色卡

---

## API 变更摘要

### 新增/修改的函数签名

```javascript
// personas.js

// 改为异步
export async function setUserAvatar(imgfile, options)
export async function autoSelectPersona(name)
export async function retriggerFirstMessageOnEmptyChat()
async function setNameCallback({ mode = 'all' }, name)
async function loadPersonaForCurrentChat({ doRender = false } = {})

// 新增导出
export function getOrCreatePersonaDescriptor()
```

### 新增/修改的导入

```javascript
// personas.js 新增
import { createOrEditCharacter, getCurrentChatId } from '../script.js';

// utils.js 新增
import { processDroppedFiles } from '../script.js';
```

### 新增变量

```javascript
// personas.js
let personaLastLoadedChatId = null;  // 跟踪最后加载的聊天 ID
```

---

## 数据结构变更

### 世界书条目 (World Info Entry)

```javascript
{
    // ... 原有字段
    outlet_name: '',  // ✨ 新增：outlet 位置名称
    // ... 其他字段
}
```

### 角色书条目 (Character Book Entry)

导出时包含：

```javascript
{
    // ... 原有字段
    outlet_name: '',  // ✨ 新增：从世界书导出时包含
    // ... 其他字段
}
```

---

## 性能优化

1. **缓存系统**：`expiredInterval: 0` - 禁用周期性过期检查
2. **重复加载防止**：通过 `personaLastLoadedChatId` 避免重复加载
3. **条件检测**：`setUserAvatar` 中检查人设是否真的改变

---

## 完整代码 Diff 文件

查看完整代码差异：

```bash
# 人设系统
git diff 4102c6af1..b2c3f749d -- public/scripts/personas.js

# 角色端点
git diff 4102c6af1..b2c3f749d -- src/endpoints/characters.js

# 群聊
git diff 4102c6af1..b2c3f749d -- public/scripts/group-chats.js

# 世界书
git diff 4102c6af1..b2c3f749d -- public/scripts/world-info.js
```

---

**文档结束**

生成时间：2025-11-01
版本：1.13.3 → 1.13.5
专注：Characters / Personas / Group Chats 系统

