# 用户过期功能 - 修改完成总结

## ✅ 修改完成

您的 `src/users.js` 文件已成功添加用户过期和活动追踪功能！

---

## 📝 已完成的修改

### 1. **添加常量定义**（第 40-50 行）

```javascript
/**
 * Set of currently online users (by handle).
 * @type {Set<string>}
 */
const ONLINE_USERS = new Set();

/**
 * Configuration for activity update interval (milliseconds).
 * Only update if last activity was more than this time ago.
 */
const ACTIVITY_UPDATE_INTERVAL = 5 * 60 * 1000; // 5 minutes
```

---

### 2. **更新 User 类型定义**（第 60-71 行）

添加了两个新属性：
```javascript
* @property {number} [expiry] - Optional expiry timestamp (milliseconds). If set, user will be logged out after this time
* @property {number} [lastActivity] - Last activity timestamp (milliseconds)
```

---

### 3. **添加辅助函数**（第 858-914 行）

#### `updateUserActivity(handle)` - 更新用户活动时间
- 每 5 分钟更新一次，避免频繁写入
- 自动将用户添加到在线列表

#### `removeOnlineUser(handle)` - 移除在线用户
- 从在线用户集合中移除指定用户

#### `getOnlineUsers()` - 获取在线用户列表（已导出）
- 返回所有在线用户的 handle 数组

#### `isUserOnline(handle)` - 检查用户是否在线（已导出）
- 返回布尔值

---

### 4. **重写 setUserDataMiddleware**（第 916-997 行）

完全按照您原来的逻辑实现，包含：

✅ **Try-catch 错误处理**
```javascript
try {
    // ... 所有逻辑
} catch (error) {
    console.error('Error in setUserDataMiddleware:', error);
    request.user = null;
    return next();
}
```

✅ **用户过期检查**
```javascript
if (profile.expiry && profile.expiry < Date.now()) {
    console.warn(`User ${profile.handle} has expired. Logging out.`);
    removeOnlineUser(profile.handle);
    // 清除并销毁会话
    request.session.handle = null;
    request.session.csrfToken = null;
    request.session.destroy(...);
    return response.redirect('/login?error=expired');
}
```

✅ **活动时间更新**
```javascript
// 异步更新，不阻塞请求
updateUserActivity(profile.handle).catch(err => {
    console.error('Failed to update user activity:', err);
});
```

✅ **自动登录尝试**
```javascript
if (await tryAutoLogin(request, globalThis.COMMAND_LINE_ARGS?.basicAuthMode)) {
    return next();
}
```

✅ **Session touch**
```javascript
if (request.method === 'GET' && request.path === '/') {
    request.session.touch = Date.now();
}
```

---

## 🎯 功能特性

### ✨ 用户过期管理
- 支持为用户设置过期时间
- 过期后自动登出并重定向到登录页
- 显示友好的过期提示（`/login?error=expired`）

### 📊 活动追踪
- 自动记录用户最后活动时间
- 5 分钟更新间隔，避免频繁写入
- 异步执行，不影响请求性能

### 👥 在线用户管理
- 实时跟踪在线用户
- 提供查询 API（`getOnlineUsers()`, `isUserOnline()`）
- 用户登出/过期时自动清理

### 🔒 会话管理
- 完整的会话清理逻辑
- 支持会话销毁
- 防止会话泄漏

---

## 🧪 测试方法

### 1. 设置用户过期（测试）

在 Node.js 控制台或代码中：

```javascript
const storage = require('node-persist');
await storage.init({ dir: './data/_storage' });

// 设置用户 1 分钟后过期
const user = await storage.getItem('user:你的用户名');
user.expiry = Date.now() + 60000; // 1 分钟
await storage.setItem('user:你的用户名', user);

console.log('用户将在 1 分钟后过期');
```

### 2. 查看在线用户

```javascript
import { getOnlineUsers, isUserOnline } from './src/users.js';

// 获取所有在线用户
console.log('在线用户:', getOnlineUsers());

// 检查特定用户
console.log('用户是否在线:', isUserOnline('username'));
```

### 3. 清除过期时间

```javascript
const user = await storage.getItem('user:你的用户名');
delete user.expiry;
await storage.setItem('user:你的用户名', user);
```

---

## 📚 API 使用示例

### 设置用户 24 小时后过期

```javascript
async function setUserExpiry(handle, hours) {
    const user = await storage.getItem(`user:${handle}`);
    if (user) {
        user.expiry = Date.now() + (hours * 60 * 60 * 1000);
        await storage.setItem(`user:${handle}`, user);
        console.log(`User ${handle} will expire in ${hours} hours`);
    }
}

// 使用
await setUserExpiry('username', 24);
```

### 查看用户活动信息

```javascript
async function getUserActivity(handle) {
    const user = await storage.getItem(`user:${handle}`);
    if (user) {
        return {
            handle: user.handle,
            name: user.name,
            lastActivity: user.lastActivity 
                ? new Date(user.lastActivity).toISOString() 
                : 'never',
            expiry: user.expiry 
                ? new Date(user.expiry).toISOString() 
                : 'never',
            isOnline: isUserOnline(handle),
        };
    }
}

// 使用
const activity = await getUserActivity('username');
console.log(activity);
```

---

## 🔧 配置说明

### 活动更新间隔

默认为 5 分钟（300,000 毫秒）。如需修改：

```javascript
// 在 src/users.js 第 50 行
const ACTIVITY_UPDATE_INTERVAL = 5 * 60 * 1000; // 改为你需要的时间
```

### 在线用户数据

`ONLINE_USERS` 存储在内存中，服务器重启后会清空。如需持久化，可以：

1. 定期保存到文件
2. 使用数据库
3. 使用 Redis 等缓存服务

---

## ⚠️ 注意事项

### 1. 时间戳格式
- 所有时间戳使用 UTC 毫秒（`Date.now()`）
- 存储和比较都是毫秒级时间戳

### 2. 性能优化
- ✅ 活动时间每 5 分钟更新一次
- ✅ 使用异步更新，不阻塞请求
- ✅ 只在有 session 时才检查

### 3. 安全性
- 过期检查在每个请求时执行
- 过期后立即清除会话
- 防止已过期用户继续访问

### 4. 兼容性
- 完全兼容现有的自动登录逻辑
- 不影响 Authelia/Authentik SSO
- 向后兼容没有过期时间的用户

---

## 🔍 调试技巧

### 查看用户数据

```javascript
const storage = require('node-persist');
await storage.init({ dir: './data/_storage' });

const user = await storage.getItem('user:username');
console.log('用户信息:', {
    handle: user.handle,
    enabled: user.enabled,
    expiry: user.expiry ? new Date(user.expiry) : 'never',
    lastActivity: user.lastActivity ? new Date(user.lastActivity) : 'never',
});
```

### 启用调试日志

代码中已包含 `console.debug` 日志，启动时设置环境变量：

```bash
# Windows PowerShell
$env:NODE_DEBUG='*'
node server.js

# Linux/Mac
NODE_DEBUG='*' node server.js
```

---

## 📦 完整功能清单

- ✅ 用户过期时间支持
- ✅ 自动登出过期用户
- ✅ 用户活动时间追踪
- ✅ 在线用户实时管理
- ✅ 完整的会话清理
- ✅ 错误处理和日志
- ✅ 性能优化（减少写入）
- ✅ 异步非阻塞执行
- ✅ TypeScript 类型支持
- ✅ 兼容现有登录流程
- ✅ 无 linter 错误

---

## 🎉 修改总结

所有修改已完成并通过 linter 检查！

**修改的位置：**
1. 第 40-50 行：添加常量
2. 第 69-70 行：User typedef 更新
3. 第 858-914 行：辅助函数
4. 第 916-997 行：setUserDataMiddleware 重写

**没有破坏性改动：**
- 保留了所有原有功能
- 完全兼容现有代码
- 新功能是可选的（不设置 expiry 就不检查）

---

## 📞 后续支持

如需添加其他功能：
- 用户管理 API 端点
- 前端登录页面错误提示
- 定期清理不活跃用户
- 在线用户列表显示

请参考之前生成的完整文档：
- `用户过期和活动追踪实现方案.md`
- `用户过期功能-完整代码.js`
- `用户过期功能-快速集成指南.md`

---

**修改完成时间**: 2025-11-01  
**文件**: `src/users.js`  
**状态**: ✅ 已完成，无错误

